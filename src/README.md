# Source

The main integrated Python implementation is:

`Microgrid_Integrated_RealIndustrial_Fault_Aware.py`

It contains the independent islanded microgrid/BESS model together with the real-industrial-AHU integration modules.

The Colab notebook version is stored under `notebooks/`.

The raw industrial dataset is deliberately excluded from this repository.
